# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'sampleImage' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire', '~> 4.5'
  # Pods for sampleImage
  target 'sampleImageTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'sampleImageUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
